BRC Cloud Admin Edge Function - Build 2026.08.14.04.11

Purpose:
- Lets an authorized BRC administrator reset a Supabase user's password.
- Lets an authorized BRC administrator update Cloud Permissions without exposing a secret key in the browser.

Function included:
  supabase/functions/admin-user/index.ts

Deploy/redeploy with Supabase CLI from the project folder:
  supabase login
  supabase link --project-ref wbitcssptqllibnfcxsg
  supabase functions deploy admin-user

No Secret Key should be copied into the BRC web build.

Dashboard / CLI authentication setting:
- Verify JWT with legacy secret: OFF
- supabase/config.toml is set to verify_jwt = false so future CLI deploys do not silently re-enable legacy verification.
